\chapter{Experiments}

\section{Arm Calibration}
This section details the calibration process undertaken to validate the robotic
arm's positional accuracy and the correctness of the inverse kinematics solver.
The primary objective is to verify that the robotic arm functions as intended,
specifically checking whether the arm can accurately traverse to a set of desired Cartesian poses (position and orientation) within its workspace. This validation is essential to bridge the gap between the theoretical kinematic model and the physical reality of the constructed manipulator. Discrepancies often arise due to factors such as manufacturing tolerances in 3D-printed components, assembly variances, and potential backlash in gear reductions. Consequently, the theoretical mapping from joint angles to Cartesian coordinates must be validated to ensure reliable operation and precise motion control.

The calibration methodology involves a systematic "pose-check" routine. First, a set of target Cartesian 
coordinates $(x, y, z)$ distributed throughout the manipulator's reachable workspace are defined in the control software. 
Second, the Inverse Kinematics (IK) solver processes these targets to compute the required joint 
angles ($\theta_1, \theta_2, \dots$) necessary to achieve the desired end-effector position. Third, these angular 
values are converted into discrete motor steps and transmitted to the actuators to drive the physical arm. Finally, 
the actual resulting pose of the end-effector is visually and physically inspected against the calculated projection. 
This allows for the verification of link lengths and joint offsets in the software model, ensuring the physical motion 
aligns with the calculated trajectory. The implementation of the inverse kinematics solver and the associated calibration 
routines are available in the project's source code repository \cite{robotarmrepo}. As shown in the following images, all 
pose and accuracy checks were successfully passed. The visual comparison confirms that the physical arm accurately 
replicates the calculated poses derived from the kinematic model.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \reflectbox{\includegraphics[width=\textwidth]{pictures/pose_1.jpg}}
        \caption{Real Robot Pose 1}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/pose_mt_1.png}
        \caption{Calculated Pose 1}
    \end{minipage}
    \caption{Comparison of Real vs Calculated Pose 1}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \reflectbox{\includegraphics[width=\textwidth]{pictures/pose_2.jpg}}
        \caption{Real Robot Pose 2}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/pose_mt_2.png}
        \caption{Calculated Pose 2}
    \end{minipage}
    \caption{Comparison of Real vs Calculated Pose 2}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \reflectbox{\includegraphics[angle=-90, width=\textwidth]{pictures/pose_3.jpg}}
        \caption{Real Robot Pose 3}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/pose_mt_3.png}
        \caption{Calculated Pose 3}
    \end{minipage}
    \caption{Comparison of Real vs Calculated Pose 3}
\end{figure}

\section{End-to-End Target Acquisition}
This experiment evaluates the integrated system's ability to autonomously detect, localize, and 
approach a specific target in a simulated harvesting scenario. The process combines the computer 
vision pipeline described in Chapter 6 with the validated kinematic control system.

The procedure commences with the synchronous acquisition of raw RGB imagery and depth data, 
which is  structured into a 3D point cloud in ply formate. To facilitate target resolution, this 
volumetric data is projected onto a 2D image plane as shown on chapter 6.4 . Following this, the trained neural network 
performs instance segmentation using out trained model on the RGB frame as outlined on chapter 6.2,
yielding a binary mask that delineates the 
target morphology. The procedure is shown in the following images.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/subject_image.png}
        \caption{Raw RGB Input}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/detected_image.png}
        \caption{Detected Target}
    \end{minipage}
    \caption{Comparison of the raw RGB input and the detected target.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{pictures/mask_segmented.png}
    \caption{Binary segmentation mask generated by the neural network.}
\end{figure}






This mask is then superimposed onto the 2D projection, enabling the isolation 
of relevant pixels. By leveraging the established spatial correspondence between the 2D 
projection and the original 3D data, the system effectively back-projects the segmented 
region to retrieve the specific 3D coordinates associated with the target object. Crucially, this 2D mask is then used to filter the original 3D dataset. By projecting the mask 
boundaries back into the 3D space, the system segments the specific subset of points belonging 
to the target object, effectively isolating it from the background and surrounding clutter. 




\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/subject_image.png}
        \caption{Raw RGB}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{pictures/masked_image.png}
        \caption{2D Projection}
    \end{minipage}
    \caption{Comparison of the acquired RGB imagery and the corresponding 2D representation derived from the 3D point cloud.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{pictures/segmented_3d_plane.png}
    \caption{Visualization of the segmented target object isolated within the 3D coordinate space.}
\end{figure}




To further refine the localization precision, a vertical partitioning strategy is applied to 
the binary mask, designated as a ``centroid-filtering'' mask. The mask is segmented into a 
lower 20\% and an upper 80\% region , as illustrated in Figure \ref{fig:centroid_mask}. This filtering mask is subsequently re-applied to the 
projected data to isolate the specific subsets of points utilized for the reaching maneuver. 
The final target coordinates are derived by averaging this filtered point set. Empirical 
analysis from prior experiments confirms that the error introduced by this averaging method 
remains strictly within the operational tolerance thresholds defined for the project. From this 
isolated point cloud, the centroid coordinates are computed to determine the precise 3D grasping 
target. These coordinates are finally passed 
to the inverse kinematics solver, which drives the robot arm to the calculated position, 
completing the acquisition cycle.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{pictures/mask_dotted.png}
    \caption{Centroid-filtering mask indicating the computed target point on the segmented object.}
    \label{fig:centroid_mask}
\end{figure}






\section{Harvesting Cycle Execution}
Following the successful acquisition of target coordinates, the system executes a multi-stage 
physical harvesting routine. This cycle is designed to ensure safe interaction with the crop 
and reliable detachment.

The operation sequence commences with an approach phase where the robot arm moves to 
a "pre-grasp" waypoint, defined with a 15 cm safety offset from the calculated target location. 
This offset prevents accidental collisions during the primary transit move. Subsequently, the 
arm executes a linear approach, closing the 15 cm distance at a controlled speed to reach the 
final interaction point. Once positioned, the gripper is closed to secure the target spear, 
followed by the activation of the cutting mechanism to sever the stalk. Upon completion of the 
cut, the manipulator retracts to a neutral "stand-up" home pose, effectively extracting the 
harvested produce from the workspace and resetting the system for the next operation.

The complete operation is orchestrated through the synchronized interplay between the 
high-level Python control software and the low-level distributed Arduino firmware. The 
Python framework, available in the project repository \cite{cloudprocessor}, governs 
the initial target acquisition and rigorous validation checks. It explicitly verifies 
the kinematic reachability of the target point within the manipulator's workspace, 
factoring in the known extrinsic transformation matrix that couples the camera's pose 
to the robot's base coordinate system. Upon validating the target, the software computes 
the inverse kinematics for the approach phase, specifically calculating a "pre-grasp" 
waypoint offset by 15 cm from the target centroid to ensure a collision-free trajectory. 
These computed angular constraints are subsequently serialized and transmitted to the 
embedded system via a UART interface. On the hardware side, two distributed 
microcontrollers execute firmware, available in the repository \cite{robotarmrepo}, that continuously polls the serial buffer for incoming 
commands. Upon receipt, the firmware interprets the angular data and synthesizes the 
precise Pulse Width Modulation (PWM) signals required to drive the actuators to the 
commanded configuration.

\begin{figure}[H]
    \centering
    \small
    \begin{verbatim}
=== Testing Robot Serial Ports ===
[OK] COM11 - OK (can open and close)
[OK] COM10 - OK (can open and close)

Opening robot serial ports...
[OK] Connected to COM11 (Joint 1 - theta1)
[OK] Connected to COM10 (Joint 2 - theta2)
[OK] Robot serial ports ready
Robot arm parameters: L1=214.0mm, L2=184.0mm
Workspace range: 30.0mm to 398.0mm from origin

Setting initial robot position (upright, angles 0, 0)...
[OK] Sent angles: theta1=0.0, theta2=0.0
[OK] Initial position set

=== Robot Arm Position-to-Angle Controller ===
Enter target X,Y coordinates in millimeters (mm) and the system will:
1. Calculate inverse kinematics
2. Send joint angles to serial ports
3. Show real-time visualization
4. Port1 (COM11) = Joint 1 angle (theta1)
5. Port2 (COM10) = Joint 2 angle (theta2)

Enter X coordinate in mm (or 'q' to quit): 200
Enter Y coordinate in mm: 150

Target position: (200.0mm, 150.0mm)
[DEBUG] IK Debug: cos_theta2=-0.2145
[DEBUG] IK Debug: theta2_elbow_up=102.39, theta2_elbow_down=-102.39
[DEBUG] IK Debug: Selected solution: elbow_up
[DEBUG] IK Debug: Robot angles (0=UP, CCW+): theta1=45.21, theta2=-102.39

[WAIT]  Ready to send data to robot:
   theta1 = 45.2 -> COM11
   theta2 = -102.4 -> COM10

Press Enter to SEND data, or 's' to SKIP: 
[OK] Sent angles: theta1=45.2, theta2=-102.4
[PLOT] Visualization displayed for target (200.0mm, 150.0mm)
------------------------------------------------------------
Enter X coordinate in mm (or 'q' to quit): 500
Enter Y coordinate in mm: 500
    \end{verbatim}
    \caption{Terminal log output showing the initialization, inverse kinematics calculation, and serial communication execution during a harvesting cycle.}
    \label{fig:terminal_log}
\end{figure}




To assess the robustness and operational stability of the system, this experimental cycle was replicated multiple times with the target asparagus placed at various positions across the reachable workspace. In the absence of external metrology equipment, the positioning error---defined as the discrepancy between the commanded target coordinates and the physical end-effector location---was evaluated via visual estimation. These observations indicated that the system consistently achieved satisfactory accuracy, with the end-effector successfully aligning with the target to facilitate reliable grasping and cutting. A video demonstration of the complete autonomous harvesting cycle can be found in \cite{harvestingdemo}.









